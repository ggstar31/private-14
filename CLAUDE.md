# CLAUDE.md

## Project Overview

**private-14** is a test application repository in its initial stage. It currently contains only a README and has no source code, dependencies, or build infrastructure set up yet.

## Repository Structure

```
/
├── README.md          # Project title and description
└── CLAUDE.md          # This file — guidance for AI assistants
```

## Current State

- Single initial commit on `master`
- No programming language, framework, or runtime has been chosen
- No package manager, dependencies, or build system configured
- No source code, tests, or CI/CD pipelines present
- No linting, formatting, or type-checking tooling configured

## Development Workflow

No build, test, or lint commands are available yet. As the project develops, update this section with:

- **Build:** `<command to build>`
- **Test:** `<command to run tests>`
- **Lint:** `<command to lint>`
- **Format:** `<command to format>`

## Conventions

Since this project is in its initial stage, no conventions have been established. When adding code, document the chosen conventions here, including:

- Language and runtime version
- Project structure and module organization
- Naming conventions (files, variables, functions, classes)
- Testing patterns (framework, file naming, coverage expectations)
- Error handling approach
- Git branch and commit message conventions

## Environment Setup

No environment setup is required at this time. Update this section when dependencies or environment variables are introduced.

## Notes for AI Assistants

- This is a greenfield project — check for new files and structure changes before making assumptions
- Always read this file at the start of a session as it will be updated as the project evolves
- When adding new infrastructure (package manager, framework, CI), update this file accordingly
- Follow any conventions established in existing code rather than introducing new patterns
